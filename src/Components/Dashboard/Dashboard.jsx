
import PropTypes from 'prop-types';

const Dashboard = props => {
    return (
        <div>
<h2>yo its zoom x belgii</h2>
        </div>
    );
};

Dashboard.propTypes = {
    
};

export default Dashboard;