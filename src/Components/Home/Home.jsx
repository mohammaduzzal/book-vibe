
import PropTypes from 'prop-types';
import Banner from '../Banner/Banner';

const Home = props => {
    return (
        <div>
            <Banner></Banner>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;